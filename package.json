{
	"name": "monorepo-demo4",
	"private": true,
	"scripts": {
		"dev": "pnpm -r dev",
		"dev:proxy": "pnpm --filter @mf/nuxt-app exec microfrontends proxy --local-apps monorepo-demo4-nuxt-app monorepo-demo4-vue-app --port 3000",
		"build": "pnpm -r build",
		"lint": "pnpm -r lint",
		"clean": "pnpm -r exec rm -rf dist node_modules .next .nuxt out"
	},
	"devDependencies": {
		"typescript": "^5.6.2"
	}
}
